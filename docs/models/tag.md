# Tag Model

The tag model will contain one piece of tagging data.

## Model definition

```js
{
    taggerId,
    tag:{
        developmentType,
        washoverType,
        impactType,
        terrianType
    }
    timeOfTag
}
```